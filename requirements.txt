# ───────── core ─────────
streamlit==1.45.0
mysql-connector-python>=8.4.0   # ← NEW: MySQL driver
pandas

# ───────── Excel helpers ─────────
xlsxwriter
openpyxl

# ───────── Auth / security ─────────
authlib>=1.3.2
bcrypt==4.1.2

# ───────── Viz / images ─────────
plotly>=5.19
Pillow>=10.2          # already present, keep

# ───────── Dev / test ─────────
pytest>=7.0
streamlit-autorefresh          # NEW utility (no pin)

# (psycopg2-binary removed – Postgres no longer used)
psycopg2-binary
